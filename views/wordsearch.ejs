<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="./css/styles.css" />
		<title>Easy</title>

		<style type="text/css">
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			body {
				display: flex;
				flex-direction: column;
				align-content: space-between;
				justify-content: flex-start;
			}



			   .header{
                grid-template-columns: repeat(2,3fr);
                grid-template-rows: repeat(2,1fr);
                display: grid;
				position: absolute;
                width: 100%;
                max-width: 816px;
                justify-content: space-between;
				grid-column-gap: 1rem;
                margin: 1rem;
            }


			   .header2 {
                display: flex;
                justify-content: space-between;
                margin: 1rem;
            }

            .class, .grade{
                align-self: middle;
                justify-self: end;
                position: relative;
                right:4rem
            }

			 .class{
                right:5.8rem;
            }

			main {
					display: flex;
				justify-content: center;
				align-items: center;
				margin-bottom: 6rem;
				margin-top: 4rem;
				flex-direction: column;
			}
			table:first-of-type{
				margin-bottom: 4rem;
				height: 31rem;
				margin-top:3rem;
			}

			h1{
				margin-top: 2rem;
			}


			table {
				border: solid black 1px;
				text-align: center;
				width: 31rem;
			}
			table.noSpacing {
				border-spacing: 0;
				border-collapse: collapse;
			}

			.wordSearchTable {
				text-align: center;
				margin-top: 3rem;
			}

			caption {
				caption-side: bottom;
				padding: 1rem;
			}
			thead {
				border-top: hidden;
				border-left: hidden;
				border-right: hidden;
			}

			th {
				padding-bottom: 1rem;
			}
			.wordSearchMeta {
				column-span: all;
				text-align: center;
			}

			tr {
				height: 5px;
			}
			tr:first-child {
				border-top: solid black 1px;
			}
			td {
				padding: -1rem;
				text-align: center;
				text-transform: capitalize;
				font-size: 1rem;
				height: 20px;
				width: 20px;
			}


			.wordsTable {
				display: flex;
				justify-content: center;
			}

			.wordsTable table td {
				border: hidden;
			}

			footer{
				position:absolute;
				bottom:3rem;
			}

		</style>
	</head>

	<body>
		<% if (header==="name, class, date and grade") { %>
			<%- include(headerPaths.nameDateClassGradePath) -%>
		<% } %>
<% if (header==="name, class and date") { %>
			<%- include(headerPaths.nameDateClassPath) -%>
		<% } %>
		<% if (header==="name and date") { %>
		<%- include(headerPaths.nameDatePath) -%>
		<% } %>
		<% if (header==="name") { %>
		 <%- include(headerPaths.namePath) -%>
		<% } %>


		<main>
			<h1><%=title%></h1>
			<table class="wordsearch">
  <%
  for (let i = 0; i < wordSearchData.length; i++) { %>
    <tr>
      <% for (let j = 0; j < wordSearchData[i].length; j++) { %>
        <td><%= wordSearchData[i][j] %></td>
      <% } %>
    </tr>
  <% } %>
</table>


			<table width="auto">
    <thead>
        <tr>
            <th colspan="5">Can you find the words below</th>
        </tr>
    </thead>
    <tbody>
        <% for (let i = 0; i < 5; i++) { %>
        <tr>
            <% for (let j = 0; j < 5; j++) { %>
            <td>
                <%= words[i * 5 + j] %>
            </td padding="1rem">
            <% } %>
        </tr>
        <% } %>
    </tbody>
</table>
		</aside>
		<footer><span> A word search created by <%=authorName%></span> from the website wordSearchGen.com </footer>
	</body>
</html>